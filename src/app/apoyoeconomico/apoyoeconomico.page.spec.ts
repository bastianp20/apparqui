import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ApoyoeconomicoPage } from './apoyoeconomico.page';

describe('ApoyoeconomicoPage', () => {
  let component: ApoyoeconomicoPage;
  let fixture: ComponentFixture<ApoyoeconomicoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ApoyoeconomicoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
